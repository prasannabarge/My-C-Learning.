#include <stdio.h>

// Declare a volatile variable
volatile int flag = 0;

int main(void) {
    // Set the flag to 1
    flag = 1;

    // Print the flag
    printf("Flag = %d\n", flag);

    return 0;
}



//these are all  main keywords in c 